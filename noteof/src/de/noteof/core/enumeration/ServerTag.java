package de.noteof.core.enumeration;

public enum ServerTag {

    // Header for registration at the server
    REQ_REGISTRATION(),
    RESP_REGISTRATION(),

    REQ_TYPE_NAME(),
    RESP_TYPE_NAME(),

    REQ_SERVICE_ID(),
    RESP_SERVICE_ID(),

    REQ_SERVICE(),
    RESP_SERVICE(),

    // Values
    VAL_OK()

}
